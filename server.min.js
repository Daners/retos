var express=require("express"),path=require("path"),bodyParser=require("body-parser"),http=require("http"),app=express();app.use(bodyParser.json());app.use(bodyParser.urlencoded({extended:!1}));app.use(express["static"](path.join(__dirname,"src")));var port=process.env.PORT||"8000";app.set("port",port);var server=http.createServer(app);server.listen(port);
